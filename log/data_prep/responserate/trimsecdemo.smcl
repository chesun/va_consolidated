------------------------------------------------------------------------------------------------------------------------
      name:  trimsecdemo
       log:  /home/research/ca_ed_lab/projects/common_core_va/consolidated/log/data_prep/responserate/trimsecdemo.smcl
  log type:  text
 opened on:  26 May 2026, 22:29:42

. 
. ********************************************************************************
. /* rename variables in the secondary demographics datasets to indicate year, keep only vars needed to calculate respon
> se rates */
. local grades `" "9" "10" "11" "12" "' //local macro for grades

. 
. use $caschls_projdir/dta/demographics/secondary/secdemo1415, replace

. keep cdscode svygr9 svygr10 svygr11 svygr12 gr9enr gr10enr gr11enr gr12enr

. //renaming vars to indicate the year of the dataset to prepare for merging with other years later
. foreach i of local grades {
  2.   rename svygr`i' svygr`i'_1415
  3.   rename gr`i'enr enrgr`i'_1415
  4.   label var svygr`i'_1415 "number of grade `i' responses in 1415"
  5.   label var enrgr`i'_1415 "grade `i' enrollment in 1415"
  6. }

. label data "trimmed secondary demographics 1415 including only survey response and enrollment for grades 9-12"
note: label truncated to 80 characters

. compress
  (0 bytes saved)

. save $datadir_clean/calschls/demotrim/secondary/trimsecdemo1415, replace
file
    /home/research/ca_ed_lab/projects/common_core_va/consolidated/data/cleaned/calschls/demotrim/secondary/trimsecdemo
    > 1415.dta saved

. 
. 
. use $caschls_projdir/dta/demographics/secondary/secdemo1516, replace
(2015-16 secondary CHKS data)

. keep cdscode svygr9 svygr10 svygr11 svygr12 gr9enr gr10enr gr11enr gr12enr

. foreach i of local grades {
  2.   rename svygr`i' svygr`i'_1516
  3.   rename gr`i'enr enrgr`i'_1516
  4.   label var svygr`i'_1516 "number of grade `i' responses in 1516"
  5.   label var enrgr`i'_1516 "grade `i' enrollment in 1516"
  6. }

. label data "trimmed secondary demographics 1516 including only survey response and enrollment for grades 9-12"
note: label truncated to 80 characters

. compress
  (0 bytes saved)

. save $datadir_clean/calschls/demotrim/secondary/trimsecdemo1516, replace
file
    /home/research/ca_ed_lab/projects/common_core_va/consolidated/data/cleaned/calschls/demotrim/secondary/trimsecdemo
    > 1516.dta saved

. 
. 
. use $caschls_projdir/dta/demographics/secondary/secdemo1617, replace
(2016-17 Secondary CHKS Data)

. keep cdscode svygr9 svygr10 svygr11 svygr12 gr9enr gr10enr gr11enr gr12enr

. foreach i of local grades {
  2.   rename svygr`i' svygr`i'_1617
  3.   rename gr`i'enr enrgr`i'_1617
  4.   label var svygr`i'_1617 "number of grade `i' responses in 1617"
  5.   label var enrgr`i'_1617 "grade `i' enrollment in 1617"
  6. }

. label data "trimmed secondary demographics 1617 including only survey response and enrollment for grades 9-12"
note: label truncated to 80 characters

. compress
  (0 bytes saved)

. save $datadir_clean/calschls/demotrim/secondary/trimsecdemo1617, replace
file
    /home/research/ca_ed_lab/projects/common_core_va/consolidated/data/cleaned/calschls/demotrim/secondary/trimsecdemo
    > 1617.dta saved

. 
. 
. use $caschls_projdir/dta/demographics/secondary/secdemo1718, replace
(2017-18 Secondary CHKS Data)

. keep cdscode svygr9 svygr10 svygr11 svygr12 gr9enr gr10enr gr11enr gr12enr

. foreach i of local grades {
  2.   rename svygr`i' svygr`i'_1718
  3.   rename gr`i'enr enrgr`i'_1718
  4.   label var svygr`i'_1718 "number of grade `i' responses in 1718"
  5.   label var enrgr`i'_1718 "grade `i' enrollment in 1718"
  6. }

. label data "trimmed secondary demographics 1718 including only survey response and enrollment for grades 9-12"
note: label truncated to 80 characters

. compress
  (0 bytes saved)

. save $datadir_clean/calschls/demotrim/secondary/trimsecdemo1718, replace
file
    /home/research/ca_ed_lab/projects/common_core_va/consolidated/data/cleaned/calschls/demotrim/secondary/trimsecdemo
    > 1718.dta saved

. 
. 
. use $caschls_projdir/dta/demographics/secondary/secdemo1819, replace
(2018-19 Secondary CHKS Data)

. keep cdscode svygr9 svygr10 svygr11 svygr12 gr9enr gr10enr gr11enr gr12enr

. foreach i of local grades {
  2.   rename svygr`i' svygr`i'_1819
  3.   rename gr`i'enr enrgr`i'_1819
  4.   label var svygr`i'_1819 "number of grade `i' responses in 1819"
  5.   label var enrgr`i'_1819 "grade `i' enrollment in 1819"
  6. }

. label data "trimmed secondary demographics 1819 including only survey response and enrollment for grades 9-12"
note: label truncated to 80 characters

. compress
  (0 bytes saved)

. save $datadir_clean/calschls/demotrim/secondary/trimsecdemo1819, replace
file
    /home/research/ca_ed_lab/projects/common_core_va/consolidated/data/cleaned/calschls/demotrim/secondary/trimsecdemo
    > 1819.dta saved

. ********************************************************************************
. 
. 
. 
. cap log close trimsecdemo
